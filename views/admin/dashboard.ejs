<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SNACK STORM</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
   <link rel="stylesheet" href="/style/admindashboard.css">
</head>

<body>
    <div class="dashboard-container">
        <!-- Main Content -->
        <main class="main-content">
            <!-- Header -->
            <div class="dashboard-header">
                <div class="welcome-section">
                    <h1>Welcome back, Admin</h1>
                    <p>Here's what's happening with your store today</p>
                </div>
            </div>

            <!-- Stats Grid -->
            <div class="stats-container">
                <!-- Total Users -->
                <div class="stat-card users-card">
                    <div class="card-header">
                        <div>
                            <p>Total Users</p>
                        </div>
                        <div class="card-icon">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                            </svg>
                        </div>
                    </div>
                    <h3><%= stats.totalUsers %></h3>
                    <div class="stat-change <%= parseFloat(stats.userChange) >= 0 ? 'positive-change' : 'negative-change' %>">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="<%= parseFloat(stats.userChange) >= 0 ? 'M5 15l7-7 7 7' : 'M19 9l-7 7-7-7' %>" />
                        </svg>
                        <%= parseFloat(stats.userChange) >= 0 ? '+' : '' %><%= stats.userChange %>% from last month
                    </div>
                </div>

                <!-- Revenue -->
                <div class="stat-card revenue-card">
                    <div class="card-header">
                        <div>
                            <p>Revenue</p>
                        </div>
                        <div class="card-icon">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                        </div>
                    </div>
                    <h3>₹<%= stats.totalRevenue %></h3>
                    <div class="stat-change <%= parseFloat(stats.revenueChange) >= 0 ? 'positive-change' : 'negative-change' %>">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="<%= parseFloat(stats.revenueChange) >= 0 ? 'M5 15l7-7 7 7' : 'M19 9l-7 7-7-7' %>" />
                        </svg>
                        <%= parseFloat(stats.revenueChange) >= 0 ? '+' : '' %><%= stats.revenueChange %>% from last month
                    </div>
                </div>

                <!-- Orders -->
                <div class="stat-card orders-card">
                    <div class="card-header">
                        <div>
                            <p>Orders</p>
                        </div>
                        <div class="card-icon">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                            </svg>
                        </div>
                    </div>
                    <h3><%= stats.totalOrders %></h3>
                    <div class="stat-change <%= parseFloat(stats.orderChange) >= 0 ? 'positive-change' : 'negative-change' %>">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="<%= parseFloat(stats.orderChange) >= 0 ? 'M5 15l7-7 7 7' : 'M19 9l-7 7-7-7' %>" />
                        </svg>
                        <%= parseFloat(stats.orderChange) >= 0 ? '+' : '' %><%= stats.orderChange %>% from last month
                    </div>
                </div>

                <!-- Products Sold -->
                <div class="stat-card products-card">
                    <div class="card-header">
                        <div>
                            <p>Products Sold</p>
                        </div>
                        <div class="card-icon">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
                            </svg>
                        </div>
                    </div>
                    <h3><%= stats.totalProductsSold %></h3>
                    <div class="stat-change <%= parseFloat(stats.conversionRate) >= 0 ? 'positive-change' : 'negative-change' %>">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
                        </svg>
                        Conversion Rate: <%= stats.conversionRate %>%
                    </div>
                </div>
            </div>

            <!-- Chart Section -->
            <div class="chart-section">
                <div class="chart-header">
                    <h3 class="chart-title">Sales & Orders Overview</h3>
                    <div class="chart-period">
                        <button id="dailyBtn" class="period-btn active">Daily</button>
                        <button id="weeklyBtn" class="period-btn">Weekly</button>
                        <button id="monthlyBtn" class="period-btn">Monthly</button>
                        <button id="yearlyBtn" class="period-btn">Yearly</button>
                    </div>
                </div>
                <div class="chart-container">
                    <canvas id="salesChart" 
                        data-labels="<%= JSON.stringify(graphData.labels) %>"
                        data-orders="<%= JSON.stringify(graphData.orders) %>"
                        data-revenue="<%= JSON.stringify(graphData.revenue) %>"
                        data-period="<%= graphData.period || 'daily' %>">
                    </canvas>
                </div>
            </div>

            <!-- Top Products and Categories Section -->
            <div class="products-categories-container">
                <!-- Top Products -->
                <div class="data-card">
                    <h3>Top Selling Products</h3>
                    <div style="overflow-x: auto;">
                        <table class="data-table">
                            <thead>
                                <tr>
                                    <th>Product</th>
                                    <th style="text-align: center;">Units</th>
                                    <th style="text-align: right;">Revenue</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (topProducts && topProducts.length > 0) { %>
                                    <% topProducts.forEach((product) => { %>
                                        <tr>
                                            <td>
                                                <div class="product-row">
                                                    <div class="product-image">
                                                        <% if (product.productInfo && product.productInfo.image && product.productInfo.image.length > 0) { %>
                                                            <img src="/uploads/<%= product.productInfo.image[0] %>" alt="<%= product.productName %>">
                                                        <% } else { %>
                                                            <div class="product-image-placeholder">
                                                                <svg width="24" height="24" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                                                                </svg>
                                                            </div>
                                                        <% } %>
                                                    </div>
                                                    <div class="product-details">
                                                        <div class="product-name"><%= product.productName %></div>
                                                        <div class="product-meta">
                                                            <span>Price: ₹<%= product.totalRevenue.toFixed(2) %></span>
                                                            <% if (product.productInfo) { %>
                                                                <span class="product-category"><%= product.productInfo.category %></span>
                                                            <% } %>
                                                        </div>
                                                    </div>
                                                </div>
                                            </td>
                                            <td style="text-align: center;">
                                                <span class="sales-badge units-badge"><%= product.totalSold %></span>
                                            </td>
                                            <td style="text-align: right;">
                                                <span class="revenue-badge">₹<%= product.totalRevenue.toFixed(2) %></span>
                                            </td>
                                        </tr>
                                    <% }); %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="3" style="text-align: center;">No products data available</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- Top Categories -->
                <div class="data-card">
                    <h3>Top Categories</h3>
                    <div style="overflow-x: auto;">
                        <table class="data-table">
                            <thead>
                                <tr>
                                    <th>Category</th>
                                    <th style="text-align: center;">Units</th>
                                    <th style="text-align: right;">Revenue</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (topCategories && topCategories.length > 0) { %>
                                    <% topCategories.forEach((category) => { %>
                                        <tr>
                                            <td>
                                                <div class="category-row">
                                                    <div class="category-icon">
                                                        <%= category._id.charAt(0).toUpperCase() %>
                                                    </div>
                                                    <div class="product-name"><%= category._id %></div>
                                                </div>
                                            </td>
                                            <td style="text-align: center;">
                                                <span class="sales-badge units-badge"><%= category.totalSold %></span>
                                            </td>
                                            <td style="text-align: right;">
                                                <span class="revenue-badge">₹<%= category.totalRevenue.toFixed(2) %></span>
                                            </td>
                                        </tr>
                                    <% }); %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="3" style="text-align: center;">No category data available</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </main>
        
        <!-- Sidebar -->
    <aside class="sidebar">
        <div class="sidebar-header">
            <a href="#" class="sidebar-logo">Admin Panel</a>
        </div>
        <nav>
            <ul class="nav-menu">
                <li class="nav-item">
                    <a href="/admin/dashboard" class="nav-link active">
                        <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                        </svg>
                        Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/salesreport" class="nav-link">
                        <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                                d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                        </svg>
                        Sales Report
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/users" class="nav-link">
                        <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                        </svg>
                        Users
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/products" class="nav-link">
                        <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
                        </svg>
                        Products
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/order" class="nav-link">
                        <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                        </svg>
                        Orders
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/category" class="nav-link">
                        <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z" />
                        </svg>
                        Categories
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/coupon" class="nav-link">
                        <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M15 5v2m0 4v2m0 4v2M5 5a2 2 0 00-2 2v3a2 2 0 110 4v3a2 2 0 002 2h14a2 2 0 002-2v-3a2 2 0 110-4V7a2 2 0 00-2-2H5z" />
                        </svg>
                        Coupons
                    </a>
                </li>
            </ul>

            <!-- Bottom Menu -->
            <ul class="bottom-menu">
                <li class="nav-item">
                    <a href="#" class="nav-link">
                        <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                        </svg>
                        Settings
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/admin/logout" class="nav-link logout-link">
                        <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                        </svg>
                        Logout
                    </a>
                </li>
            </ul>
        </nav>
    </aside>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const canvas = document.getElementById('salesChart');
            const ctx = canvas.getContext('2d');
            let chart;
            const labels = JSON.parse(canvas.dataset.labels);
            const orderData = JSON.parse(canvas.dataset.orders);
            const revenueData = JSON.parse(canvas.dataset.revenue);
            
            function initChart(labels, orderData, revenueData) {
             
                if (chart) {
                    chart.destroy();
                }
                
                chart = new Chart(ctx, {
                    type: 'line',
                    data: {
                        labels: labels,
                        datasets: [
                            {
                                label: 'Orders',
                                data: orderData,
                                borderColor: '#8b5cf6', 
                                backgroundColor: 'rgba(139, 92, 246, 0.1)',
                                tension: 0.3,
                                yAxisID: 'y',
                                pointBackgroundColor: '#8b5cf6',
                                pointRadius: 4,
                                borderWidth: 3
                            },
                            {
                                label: 'Revenue (₹)',
                                data: revenueData,
                                borderColor: '#10b981', 
                                backgroundColor: 'rgba(16, 185, 129, 0.1)',
                                tension: 0.3,
                                yAxisID: 'y1',
                                pointBackgroundColor: '#10b981',
                                pointRadius: 4,
                                borderWidth: 3
                            }
                        ]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        interaction: {
                            mode: 'index',
                            intersect: false,
                        },
                        scales: {
                            y: {
                                type: 'linear',
                                display: true,
                                position: 'left',
                                title: {
                                    display: true,
                                    text: 'Orders'
                                }
                            },
                            y1: {
                                type: 'linear',
                                display: true,
                                position: 'right',
                                grid: {
                                    drawOnChartArea: false,
                                },
                                title: {
                                    display: true,
                                    text: 'Revenue (₹)'
                                }
                            }
                        },
                        plugins: {
                            tooltip: {
                                callbacks: {
                                    label: function(context) {
                                        let label = context.dataset.label || '';
                                        if (label) {
                                            label += ': ';
                                        }
                                        if (context.dataset.label === 'Revenue (₹)') {
                                            label += '₹' + context.parsed.y.toFixed(2);
                                        } else {
                                            label += context.parsed.y;
                                        }
                                        return label;
                                    }
                                }
                            }
                        }
                    }
                });
            }
            initChart(labels, orderData, revenueData);
        
            const buttons = {
                daily: document.getElementById('dailyBtn'),
                weekly: document.getElementById('weeklyBtn'),
                monthly: document.getElementById('monthlyBtn'),
                yearly: document.getElementById('yearlyBtn')
            };
            
            function setActiveButton(activeButton) {
                Object.values(buttons).forEach(button => {
                    button.classList.remove('bg-blue-600', 'text-white', 'active-period');
                    button.classList.add('bg-gray-200', 'text-gray-700');
                });
                
                activeButton.classList.remove('bg-gray-200', 'text-gray-700');
                activeButton.classList.add('bg-blue-600', 'text-white', 'active-period');
            }
            
            function fetchChartData(period) {
                fetch(`/admin/dashboard/chart-data?period=${period}`)
                    .then(response => {
                        if (!response.ok) {
                            throw new Error('Network response was not ok');
                        }
                        return response.json();
                    })
                    .then(data => {
                        initChart(data.labels, data.orders, data.revenue);
                    })
                    .catch(error => {
                        console.error('Error fetching chart data:', error);
                    });
            }
            
            buttons.daily.addEventListener('click', function() {
                setActiveButton(this);
                fetchChartData('daily');
            });
            
            buttons.weekly.addEventListener('click', function() {
                setActiveButton(this);
                fetchChartData('weekly');
            });
            
            buttons.monthly.addEventListener('click', function() {
                setActiveButton(this);
                fetchChartData('monthly');
            });
            
            buttons.yearly.addEventListener('click', function() {
                setActiveButton(this);
                fetchChartData('yearly');
            });
        });
    </script>